
public interface ITouchSensor {

	/**
	 * Returns the current value of the sensor.
	 * @return - Returns true if the sensor is pushed, otherwise returns false
	 */
	public boolean getIsPressed();
	
}
